# VBUS Backend

Commands:

- Install: `npm install`
- Dev: `npm run start:dev`
- Build: `npm run build`
- Start: `npm run start`

This project uses NestJS. Add your modules under `src/` and import them into `AppModule`.
