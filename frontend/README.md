# VBUS Frontend

Commands:

- Install: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`
- Start: `npm run start`

This project uses Next.js + TailwindCSS. Global styles are in `app/globals.css`.
